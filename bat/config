--color=always
#--line-numbers=always
#--file-size
--show-all
--pager="less --RAW-CONTROL-CHARS --quit-if-one-screen --mouse"
--highlight-line 30:40
--line-range 30:40
--theme=OneHalfDark

# Không wrap nội dung dài
--wrap=auto

# Hiển thị dấu phân cách cho dễ nhìn
--border=horizontal

# Sử dụng icon cho file
--icons=always

# Cấu hình pager cho file dài
--pager=less

# Tùy chọn hiển thị đánh dấu cú pháp
--style=full

--completion zsh
